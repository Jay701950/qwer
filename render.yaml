services:
  - type: web
    name: fast-browser-relay
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    envVars:
      - key: PORT
        value: "8080"
      - key: PROFILE_DIR
        value: "/tmp/profiles"

{
  "name": "render-safe-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "A security-conscious allowlisted HTTP fetch proxy for Render",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "NODE_ENV=test node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "helmet": "^8.0.0"
  }
}
